root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/main ./cmd/api"
  bin = "./tmp/main"
  delay = 1000
  exclude_dir = ["tmp", "vendor", ".devcontainer"]
  include_ext = ["go"]
  exclude_regex = ["_test.go"]

[log]
  time = true

[misc]
  clean_on_exit = true
